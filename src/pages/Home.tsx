import React, { FC } from 'react';
import Header from '@/components/Header/Header';

const Home: FC = () => {
  return (
    <div>
      <Header>Home</Header>
    </div>
  );
};

export default Home;
